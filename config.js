// ============================================================
//  DOCK TRACKER v8.0 — CONFIG (GitHub Pages)
// ============================================================
window.DT_CONFIG = {
    // Caminho do JSON de dados (relativo ao index.html)
    DATA_URL: 'data/latest.json',

    // FC padrão
    FC_DEFAULT: 'REC1',

    // Auto-refresh a cada 5 min
    REFRESH_MS: 5 * 60 * 1000,

    // Versão
    VERSION: '8.0',
};
